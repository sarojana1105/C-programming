#include<stdio.h>
   //area of square

     int main(){
     int side;

    printf("Enter side: ");
    scanf("%d", &side);

    printf("Area of square = %d\n", side * side);

     return 0;
     }
     
